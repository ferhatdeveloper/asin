[package]
name = "postgresql-remote-enable"
version = "0.1.0"
edition = "2021"
description = "RetailEX — PostgreSQL LAN erişimi (pg-windows-expose-remote.ps1 UAC sarmalayıcısı)"

[[bin]]
name = "PostgreSQLRemoteEnable"
path = "src/main.rs"

[dependencies]
