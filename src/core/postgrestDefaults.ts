/**
 * PostgREST varsayılanları — postgres.ts circular import zincirinden bağımsız yaprak modül.
 * (postgrest.config → postgres TDZ: Cannot access before initialization)
 */
export const DEFAULT_POSTGREST_PORT = 3002;
